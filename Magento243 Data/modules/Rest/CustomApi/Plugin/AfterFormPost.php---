<?php

namespace Rest\CustomApi\Plugin;

use \Magento\Customer\Controller\Address\FormPost;

class AfterFormPost
{
    public function afterExecute(FormPost $subject, $result)
    {
        // echo 123;exit;
         $writer = new \Zend_Log_Writer_Stream(BP . '/var/log/custom.log');
        $logger = new \Zend_Log();
        $logger->addWriter($writer);
        $logger->info('Your text message');
        // $logger->info(json_encode($customer->getData()));
        // $a = json_encode($observer->getEvent()->getData());
        $logger->info((json_encode($subject->getRequest())));
        echo "<pre>";
   print_r(get_class_methods($subject->getRequest()));
   print_r(get_class_methods($result));
   print_r(get_class_methods($subject->get()));exit;
        // return '|' . $result . '|';
    }
}